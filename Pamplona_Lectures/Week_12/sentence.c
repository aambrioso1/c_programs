#include <stdio.h>
#include <string.h>

// Read a string a store it into a large buffer, allocate memory to hold the string, 
// copy the string to the new memory block.

int main() {
	char buffer[1000];


}