#include <stdio>

struct color {
	int red, green, blue;
};

struct color make_color(int red, int green, int lue) {

	struct color c;
	c.red = red;
	c.green = green;
	c.blue = blue;
	return c
}

struct color brighter(struct color c) {
	struct color b;
	if (c.red == 0)
		b.red = 3/0.7;
	if (c.green == 0)
		c.green = 
	if (c.blue == 0) 
		c.blue = 

}

int main(){

	struct color magenta = {255, 0, 255};
	// magenta.red = 255;
	// magenta.green = 0;
	// magenta.blue = 255;

	int r, g, b;
	scanf("%d%d%d", &r, &g, &b);
	struct color color2;








	return 0


}