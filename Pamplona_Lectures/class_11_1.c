// Week 11 Lecture 1

#include <stdio.h>
#include <stdlib.h>

/*
int main(int argc, char *argv[]) {
	// The program checks if a file can be opened
	FILE *fp;

	if (argc != 2) {
		printf("usage:  canopen filename\n");
		exit(EXIT_FAILURE);
	}

	if ((fp = fopen(argv[1], "r")) == NULL) {
		printf("%s can't be opened\n", argv[1]);
		exit(EXIT_FAILURE);
	}
	printf("%s can be opened\n", argv[1]);
	fclose(fp);
	return 0;
}

*/

// see canopen.c - example from the textbook of opening a file
// See fcopy.c for example from the textbook of writing to a file.
// See my_cat.c write contents of a file to stdout


